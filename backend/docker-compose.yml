services:
  api-gateway:
    build: "./api-gateway"
    ports:
      - "5000:5000"
    env_file:
      - "./api-gateway/.env"
    depends_on:
      - media-service
      - auth-service
    networks:
      - codeyaan-network

  auth-service:
    build: "./auth-service"
    ports:
      - "5002:5003"
    env_file:
      - "./auth-service/.env"
    networks:
      - codeyaan-network


  media-service:
    build: ./media-service
    ports:
     - "7000:7001"  
    env_file:
      - ./media-service/.env
    networks:
      - codeyaan-network
 

# 👇 existing external network use kar rahe ho
networks:
  codeyaan-network:
    driver: bridge